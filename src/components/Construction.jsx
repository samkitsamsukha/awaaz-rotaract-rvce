import React from "react";
import { Link } from "react-router-dom";

export default function Construction() {
    return (
        <nav className="w-full flex items-center justify-between px-6 py-2 shadow-md bg-pink-700 z-50 text-white">
            <div className="flex items-center gap-3">
                Work in Progress - Awaaz is getting an all new look, Stay Tuned!
            </div>
        </nav>
    );
}
